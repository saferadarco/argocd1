apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: mydotnetapp1-cronjob
spec:
  schedule: "*/2 * * * *"  # Run every 2 minutes
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: mydotnetapp1
            image: mydotnetapp1:latest
          restartPolicy: OnFailure
